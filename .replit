modules = ["nodejs-20", "web"]
run = "npx vite --host --port 8080"
hidden = [".config", ".git", "generated-icon.png", "node_modules", "dist"]

[nix]
channel = "stable-24_05"
packages = ["unzip"]

[deployment]
deploymentTarget = "static"
build = ["npx", "vite", "build"]
publicDir = "dist"

[[ports]]
localPort = 8080
externalPort = 80